# VelocX NZ — Daily Post Agent

**INDUSTRY: `velocx_nz`**

Read and follow `agents/DAILY_POST_AGENT.md` exactly.
Replace every `{INDUSTRY}` placeholder with `velocx_nz`.
The brand config is at `config/industries/velocx_nz.json`.
