name: Schedule - JL RealEstate

on:
  # schedule: PAUSED (automation paused for this brand — no automatic posting)
  #   - cron: '0 */3 * * *'   # every 3 hours — scheduler decides internally if it's a posting window
  workflow_dispatch:
    inputs:
      force:
        description: 'Force post immediately (skip timing check)'
        type: boolean
        default: false

permissions:
  contents: write
  actions: write   # needed to trigger post_JL_RealEstate.yml via gh workflow run

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  schedule:
    runs-on: ubuntu-latest
    environment: JL_RealEstate
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write credentials
        env:
          FACEBOOK_PAGE_ID: ${{ secrets.FACEBOOK_PAGE_ID }}
          FACEBOOK_ACCESS_TOKEN: ${{ secrets.FACEBOOK_ACCESS_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
        run: |
          mkdir -p config/credentials
          printf "FACEBOOK_PAGE_ID=%s\nFACEBOOK_ACCESS_TOKEN=%s\nANTHROPIC_API_KEY=%s\nINSTAGRAM_USER_ID=%s\nINSTAGRAM_ACCESS_TOKEN=%s\n" \
            "$FACEBOOK_PAGE_ID" \
            "$FACEBOOK_ACCESS_TOKEN" \
            "$ANTHROPIC_API_KEY" \
            "$INSTAGRAM_USER_ID" \
            "$INSTAGRAM_ACCESS_TOKEN" \
            > config/credentials/JL_RealEstate.env

      - name: Configure git
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}

      - name: Run content scheduler
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          if [ "${{ inputs.force }}" = "true" ]; then
            python src/content_scheduler.py --industry JL_RealEstate --force
          else
            python src/content_scheduler.py --industry JL_RealEstate
          fi
